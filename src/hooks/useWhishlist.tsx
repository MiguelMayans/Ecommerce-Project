import React, { useReducer } from 'react'

type Props = {}

const useWhishlist = (props: Props) => {


    const [state, dispatch] = useReducer(first, second, third)



    return (
        <div>useWhishlist</div>
    )
}

export default useWhishlist